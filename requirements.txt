# API dependencies
openai>=1.0.0

# Core dependencies
numpy>=1.20.0
tqdm>=4.65.0
pydantic>=2.0.0
scipy>=1.9.0

# Dataset loading (still needed for data preparation)
datasets>=2.12.0
scikit-learn>=1.0.0
huggingface_hub>=0.16.0

# Optional: uncomment if you want to use local models instead of API
# torch>=1.13.0
# transformers>=4.30.0
# accelerate>=0.20.0

# Optional: other API providers
anthropic>=0.7.0
